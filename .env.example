# Canonical site URL for metadata, sitemap, and hreflang (no trailing slash)
NEXT_PUBLIC_SITE_URL=http://localhost:3000

# Runtime base URL (Cloud Run / Docker). Overrides NEXT_PUBLIC for server routes like sitemap.
# SITE_URL=https://your-service-xxxxx.run.app
